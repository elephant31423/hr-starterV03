<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.hrstarter.mapper.AuditLogMapper">

    <insert id="insert" parameterType="com.example.hrstarter.entity.AuditLogEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO audit_logs(
                               user_id, username, action, entity_type,entity_id,old_value,new_value ,status,error_message,ip_address,user_agent)
        VALUES(
               #{userId}, #{username}, #{action}, #{entityType},#{entityId}, #{oldValue}, #{newValue}, #{status},#{errorMessage}, #{ipAddress}, #{userAgent})
    </insert>


    <select id="query"   resultType="com.example.hrstarter.entity.AuditLogEntity">
        SELECT *
        FROM audit_logs

        <where>
            <if test="username != null and username != ''">
                AND username = #{username}
            </if>
            <if test="action != null and action != ''">
                AND action = #{action}
            </if>
            <if test="entityType != null and entityType != ''">
                AND entity_type = #{entityType}
            </if>
            <if test="startDate != null">
                AND created_at >= #{startDate}
            </if>
            <if test="endDate != null">
                AND created_at &lt;= #{endDate}
            </if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{offset}, #{size}
    </select>
    <select id="count" resultType="long">
        SELECT COUNT(*)
        FROM audit_logs
        <where>
            <if test="username != null and username != ''">
                AND username = #{username}
            </if>
            <if test="action != null and action != ''">
                AND action = #{action}
            </if>
            <if test="entityType != null and entityType != ''">
                AND entity_type = #{entityType}
            </if>
            <if test="startDate != null">
                AND created_at >= #{startDate}
            </if>
            <if test="endDate != null">
                AND created_at &lt;= #{endDate}
            </if>
        </where>
    </select>



</mapper>