<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.hrstarter.mapper.LeaveRecordMapper">

    <insert id="insert">
        INSERT INTO leave_records(employee_id, start_date, end_date, days, reason, status)
        VALUES (#{employeeId}, #{startDate}, #{endDate}, #{days}, #{reason}, #{status})
    </insert>

    <select id="findByEmployeeAndDateRange"
            resultType="com.example.hrstarter.entity.LeaveRecords">
        SELECT
            employee_id,
            leave_date,
            leave_type,
            hours
        FROM leave_records
        WHERE employee_id = #{employeeId}
          AND leave_date BETWEEN #{startDate} AND #{endDate}
          AND status = 'APPROVED'
    </select>

    <update id="updateStatus">
        UPDATE leave_records
        SET status = #{status}
        WHERE id = #{id}
    </update>
</mapper>